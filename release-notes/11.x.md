# 11.0.8

## Remove dependency management pin for swagger-ui

springdoc-openapi 3.1.1 has now been released which upgrades swagger-ui to a later version than we had pinned.
Can therefore now remove the pin and allow it to float freely.

Note that this means that you should now remove
```yaml
springdoc.swagger-ui.version: 5.32.11
```

from your `application.yml`.

# 11.0.7

## Tomcat pinning for multiple vulnerabilities

Pin tomcat to 11.0.25, which fixes: CVE-2026-65637, CVE-2026-65905, CVE-2026-65182, CVE-2026-68525, CVE-2026-65183,
CVE-2026-66422, CVE-2026-68569, CVE-2026-65927, CVE-2026-68763, CVE-2026-73180

## OpenTelemetry suppression for javascript issues

Suppress CVE-2026-54285, which only affects opentelemetry-js.

# 11.0.6

## Upgrade to Spring Boot 4.1.1

Upgrade Spring Boot from 4.1.0 to 4.1.1.

Spring Boot now manages the versions previously pinned for Tomcat, Netty, Logback, Log4j2, Jackson, HttpClient and
HttpCore, so those local pins have been removed.

## Removed dependency management pins

The following local version pins have been removed as Spring Boot 4.1.1 now manages the same or newer versions:

- `tomcat.version` [11.0.24 -> 11.0.24]
- `netty.version` [4.2.16.Final -> 4.2.17.Final]
- `logback.version` [1.5.37 -> 1.5.38]
- `log4j2.version` [2.25.5 -> 2.25.5]
- `jackson-2-bom.version` [2.21.5 -> 2.21.5]
- `jackson-bom.version` [3.1.5 -> 3.1.5]
- `httpclient5.version` [5.6.4 -> 5.6.4]
- `httpcore5.version` [5.4.3 -> 5.4.3]

The explicit Jackson Kotlin module versions have also been removed and are now managed by the Spring Boot BOM:

- `tools.jackson.module:jackson-module-kotlin` [3.1.5 -> 3.1.5]
- `com.fasterxml.jackson.module:jackson-module-kotlin` [2.21.5 -> 2.21.5]

## Version upgrades

- `com.github.ben-manes.versions` Gradle plugin [0.54.0 -> 0.61.0]
- `org.springframework.boot:spring-boot-gradle-plugin` [4.1.0 -> 4.1.1]
- `org.junit.jupiter:junit-jupiter` [6.1.2 -> 6.1.3]
- `net.javacrumbs.json-unit:json-unit-assertj` [6.0.1 -> 6.2.0]
- `org.eclipse.jgit:org.eclipse.jgit` [7.7.0.202606012155-r -> 7.7.1.202607240634-r]
- `org.junit.platform:junit-platform-launcher` [6.1.2 -> 6.1.3]

## Another suppression for a dompurify vulnerability

Suppress `CVE-2026-75838` for DOMPurify bundled inside `swagger-ui-5.32.11`.

Dependency Check reports this twice, once for `swagger-ui-bundle.js` and once for `swagger-ui-es-bundle.js`. The issue
is an XSS vulnerability in DOMPurify before `3.4.13` when using `IN_PLACE` sanitization with hooks that remove elements.

We are suppressing this because DOMPurify is bundled with Swagger UI and Swagger docs should only be available in
restricted development environments. Remove this suppression when Swagger UI includes DOMPurify `3.4.13` or later.

# 11.0.5

## Fix for CVE-2026-64607

Pin httpclient5 to 5.6.4.

See:
https://nvd.nist.gov/vuln/detail/CVE-2026-64607

## Another suppression for a dompurify vulnerability

Suppress [GHSA-55q2-fjhq-7xh7](https://github.com/cure53/DOMPurify/security/advisories/GHSA-55q2-fjhq-7xh7) for
DOMPurify: IN_PLACE hook removal leaves a detached subtree executable, causing XSS.

We can't find any evidence that swagger docs are vulnerable as a result of the dompurify issue. Furthermore the docs
should be configured in your projects to only be available in the development environment. This environment should be ip
allowlist restricted too, so the scope of the vulnerability is therefore minimal.

# 11.0.4

## Suppress Tomcat example vulnerability

Suppress CVE-2026-66299 as only affects the websocket chat example and is an experiment by the tomcat security team.
See https://lists.apache.org/thread/8owczcc1o8qw1rxmg9gvfk4w2jnh4l5k for more information.

# 11.0.3

## Remove expired .snyk ignore

Removes unneeded and expired .snyk ignore.

# 11.0.2

## Pin swagger-ui to latest version 5.32.11

[CVE-2026-65898](https://nvd.nist.gov/vuln/detail/CVE-2026-65898)
[CVE-2026-65899](https://nvd.nist.gov/vuln/detail/CVE-2026-65899)
[CVE-2026-65902](https://nvd.nist.gov/vuln/detail/CVE-2026-65902)
[CVE-2026-65903](https://nvd.nist.gov/vuln/detail/CVE-2026-65903)
[CVE-2026-65900](https://nvd.nist.gov/vuln/detail/CVE-2026-65900)
[CVE-2026-65901](https://nvd.nist.gov/vuln/detail/CVE-2026-65901)
[CVE-2026-66010](https://nvd.nist.gov/vuln/detail/CVE-2026-66010)

Note that this change causes a problem in projects protected by spring security as by default the 5.32.2 version files
are unprotected. To work around that issue set:

```yaml
springdoc.swagger-ui.version: 5.32.11
```

in your `application.yml`.

# 11.0.1

## Upgrade to Kotlin 2.4.10

This includes an [upgrade to Kotlin 2.4.10](https://github.com/JetBrains/kotlin/releases/tag/v2.4.10)

## Tomcat pinning for CVE-2026-59083 and CVE-2026-59084

Pin tomcat to 11.0.24, which fixes the vulnerabilities.

See:
https://nvd.nist.gov/vuln/detail/CVE-2026-59083
https://nvd.nist.gov/vuln/detail/CVE-2026-59084

## Netty pinning for CVE-2026-56819, CVE-2026-59921 and CVE-2026-59901

Pin netty to 4.2.16.Final, which fixes the vulnerabilities.

See:
https://nvd.nist.gov/vuln/detail/CVE-2026-56819
https://nvd.nist.gov/vuln/detail/CVE-2026-59901
https://nvd.nist.gov/vuln/detail/CVE-2026-59921

## Jackson pinning SNYK-JAVA-TOOLSJACKSONCORE-17972609

Pin jackson 3 to 3.1.5, which fixes the vulnerability.

See:
https://security.snyk.io/vuln/SNYK-JAVA-TOOLSJACKSONCORE-17972609

## Log4j2 pinning for CVE-2026-49844

Pin log4j2 to 2.25.5, which fixes the vulnerabilities.

See:
https://nvd.nist.gov/vuln/detail/CVE-2026-49844

## Version upgrades

- org.junit.jupiter:junit-jupiter [6.1.1 -> 6.1.2]
- org.junit.platform:junit-platform-launcher [6.1.1 -> 6.1.2]

# 11.0.0

## Upgrade to Spring Boot 4.1.0

Upgrade to spring-boot [4.1.0](https://github.com/spring-projects/spring-boot/releases/tag/v4.1.0).

We have classified this as a major upgrade because there are potentially breaking changes to the WebClient.Builder
filter functions introduced in the release (via spring security upgrade).

Due to https://github.com/spring-projects/spring-security/issues/19324
`ServletOAuth2AuthorizedClientExchangeFilterFunction` no longer works as expected in a servlet environment when there is
no request / response in context (e.g. when called from batch jobs). This means that all of those webclient requests
fail with a `401` as oauth2 authentication is not performed first.

If you are using kotlin lib to build your web clients then this upgrade will work without any further changes as a
temporary filter fix will be included in a matching release of that project.

It is worth searching your projects for `ServletOAuth2AuthorizedClientExchangeFilterFunction` usage. If you find any
usages then you will need to add in:

```kotlin
  .filter(ServletRequestResponseNonNullFilterFunction())
```

before the call to the oauth2 filter function.

Note that reactive applications are not affected by the issue and will continue to work as expected without any changes,
since it is only the servlet version of the filter function that includes the null code change.

## Version upgrades

- net.javacrumbs.json-unit:json-unit-assertj [5.1.2 -> 6.0.1]
- org.springframework.boot:spring-boot-gradle-plugin [4.0.7 -> 4.1.0]

## Fixes for CVE-2026-54512 and CVE-2026-54513

Pin jackson 2 to version 2.15.1, which fixes the vulnerabilities.

See:
https://nvd.nist.gov/vuln/detail/CVE-2026-54512
https://nvd.nist.gov/vuln/detail/CVE-2026-54513

## Fixes for CVE-2026-54399 and CVE-2026-54428

Pin httpclient5 to 5.6.2 and httpcore5 to 5.4.3, which fixes the vulnerabilities. Note that we've pinned the httpclient5
too since that version is the one that pulls in httpcore5 so makes sense to upgrade both together.

See:
https://nvd.nist.gov/vuln/detail/CVE-2026-54399
https://nvd.nist.gov/vuln/detail/CVE-2026-54428
